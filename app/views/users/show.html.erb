<div class="page-header">
  <h1><%= @user.name %> : <i><%= @user.money %> $</i></h1>
</div>  

  <div class="container">
  <div class="btn-group "> 
  <%= link_to "Получить", recieve_user_path(@user), class: "btn btn-success btn-large "%>
<%= link_to "Снять", spend_user_path(@user), class: "btn btn-danger btn-large"%> 

<%= link_to "Перевести", transfer_user_path(@user), class: "btn btn-info btn-large"%>
<%= link_to 'Назад', users_path, class: "btn btn-default btn-large" %>
</div>
  


</div>